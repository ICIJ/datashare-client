// import {shallow} from 'vue-test-utils'
// import TiffViewer from '@/components/document/TiffViewer'

describe('TiffViewer.vue', () => {
  it('should display an empty page when tiff is not found', async () => {
    // var wrapped = shallow(TiffViewer, {propsData: {'url': 'invalid.url'}})

    // await wrapped.update()

    // expect(wrapped.isEmpty()).to.equal(true)
  })
})
